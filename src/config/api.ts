// API Configuration
// Update this URL to switch between local and production environments
export const API_BASE_URL = 'https://skilsense-back.vercel.app';

// Optional: Add timeout configuration
export const API_TIMEOUT = 30000; // 30 seconds

// Optional: Add retry configuration
export const API_RETRY_ATTEMPTS = 3;
export const API_RETRY_DELAY = 1000;
